{
  "name": "paymodel",
  "version": "0.1.0",
  "private": true,
  "description": "Crypto-native API proxy for open-source LLM models",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
